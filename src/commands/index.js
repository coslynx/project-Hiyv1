// File: src/commands/index.js (JavaScript)

const moderation = require('./moderation');
const custom = require('./custom');
const settings = require('./settings');

module.exports = {
  moderation,
  custom,
  settings,
};